# high-cpu-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: cpu-stress
  labels:
    app: cpu-stress
spec:
  containers:
    - name: stress
      image: polinux/stress
      args: ["--cpu", "1", "--timeout", "600s"]
      resources:
        limits:
          cpu: "0.5"
        requests:
          cpu: "0.5"
